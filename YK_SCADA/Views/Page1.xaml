<UserControl
    x:Class="YK_SCADA.Views.Page1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:data="clr-namespace:System.Data;assembly=System.Data"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:YK_SCADA.ViewModel"
    Name="mainview"
    Padding="10"
    d:DataContext="{d:DesignInstance Type=vm:Page1ViewModel}"
    d:DesignHeight="720"
    d:DesignWidth="1280"
    Background="Black"
    mc:Ignorable="d">

    <!--<UserControl.Background>
        <ImageBrush ImageSource="/imgs/背景deepdark.png" />
    </UserControl.Background>-->

    <UserControl.Resources>
        <Style x:Key="MyTextBlock" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="70" />
            <Setter Property="Foreground" Value="white" />
            <Setter Property="Margin" Value="0" />
        </Style>
        <Style x:Key="MyTextBlock2" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="30" />
            <Setter Property="Foreground" Value="Wheat" />
            <Setter Property="Margin" Value="0" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="DarkBlue">

  
       

       
        </Grid>

        <Grid Grid.Row="1" Background="Gold">




        </Grid>
    </Grid>
</UserControl>